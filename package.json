{
  "name": "perky-team-claude-plugin-tests",
  "private": true,
  "type": "module",
  "//engines": "Test harness only — the shipped plugins still support Node 22.5. The suite needs 24 because node:sqlite honors the file:…?immutable=1 URI there and not on 22, which p-graph's read-only-directory test depends on. See .claude/CLAUDE.md.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "node scripts/validate.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/semver": "^7.5.0",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "semver": "^7.6.0",
    "tree-sitter-wasms": "0.1.13",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0",
    "web-tree-sitter": "0.25.10"
  }
}
